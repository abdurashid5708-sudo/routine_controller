name: routine_controller
description: "A new Flutter project."
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  shared_preferences: ^2.2.2
  uuid: ^4.4.0
  flutter_local_notifications: ^22.0.1
  zo_app_blocker: ^0.0.6

  # --- ADDED: Required for reliable scheduled notifications that survive app close ---
  timezone: ^0.11.0
  flutter_timezone: ^5.1.0

  # --- ADDED: Required for background penalty checking when app is closed ---
  workmanager: ^0.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
